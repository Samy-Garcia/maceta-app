// Paleta de colores por defecto (tema claro). Los componentes deben usar
// useTheme() de ThemeContext.jsx para soportar modo oscuro; este export se
// mantiene por si algo necesita la paleta claro fuera de un componente.
export { lightColors as colors } from './palettes.js';
